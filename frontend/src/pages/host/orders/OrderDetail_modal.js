import React, { useEffect, useRef, useState } from "react";
import Swal from "sweetalert2";
import { useNavigate } from "react-router";

function OrderDetail(rownum) {
  return (
    <>
      <div>테스트 {rownum}</div>
    </>
  );
}
export default OrderDetail;
