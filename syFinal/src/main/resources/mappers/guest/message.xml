<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="message">
<select id="g_list" resultType="com.example.syFinal.global.model.MessageDTO">
	select * from message where m_g_idx = #{g_idx}
</select>

<select id="h_list" resultType="com.example.syFinal.global.model.MessageDTO">
	select * from message where m_h_idx = #{h_idx}
</select>

<insert id="create">
insert into message(m_roomId, m_h_idx, m_g_idx, m_message) values
(#{room}, #{h_idx}, #{g_idx}, #{message})
</insert>

<select id="entrance" resultType="com.example.syFinal.global.model.MessageDTO">
	select * from message where m_h_idx = #{h_idx} and m_g_idx = #{g_idx}
</select>
</mapper>